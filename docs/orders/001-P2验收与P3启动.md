# 指令 001：P2 验收通过，启动 P3（多模型对比实验）

> 发布：2026-09-05 14:02 | 发布者：resume 侧管理层
> 优先级：高 | 状态：⬜ 待执行

## 背景
管理层已验收 P0/P1/P2：
- 77 tests 全绿、覆盖率 93.16%（门禁 90% 达标）
- executor_real.py 真实 pytest 执行闭环成立（docs/experiments/p2-real-executor.md 质量达标）
- P2 文档已明确 P3 前置条件满足："只需替换 LLM 客户端并固定 seed/温度"

## 指令内容
1. **确认 P2 收尾**：如尚未回报，请按 CHARTER §7 格式回报
  （`阶段 | 完成内容 | 测试数/覆盖率 | 新增能力 | 下一步`）
2. **启动 P3 多模型对比实验**（按 CHARTER §3 研究纪律）：
   - 先写实验设计（假设/控制变量/指标/判定标准）到 docs/experiments/p3-multimodel.md
   - 模型建议：DeepSeek（deepseek-chat）为主 + 至少 1 个对照（如 mock 基线 或 另一模型）
   - 固定 seed/温度；同一批需求输入；三指标（parse_rate/exec_pass/quality）对比
   - 跑完出结论 + 可复现命令
3. **若缺 DeepSeek API key**：不要阻塞——先把实验设计+脚本写好（mock 可跑通），
   在 docs/orders/STATUS.md 回报"待 key"，管理层协调用户提供

## 纪律提醒
- 数字实测、禁止编造（铁律 1）
- 实验先写假设再动手（研究纪律 1）
- 每个结论配可复现命令（研究纪律 3）

## 回报方式
完成或遇阻后：在 `docs/orders/STATUS.md` 写入状态，并更新本文件状态为 ✅/⛔
